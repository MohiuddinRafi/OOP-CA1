/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Main.java to edit this template
 */
package main;

import javax.swing.JOptionPane;

/**
 *
 * @author rafim
 */
public class Main {

    private static MedicationTracker medicationTracker;

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        // TODO code application logic here
        MedGUI myGUI = new MedGUI();
        myGUI.setVisible(true);

       /* String medicationNames;
        String dosages;
        int timesPerDay;
        String intakeInstructions;

       
            medicationNames = JOptionPane.showInputDialog("Enter name for medication " + ":");
            dosages = JOptionPane.showInputDialog("Enter dosage for medication "  + ":");
            timesPerDay = Integer.parseInt(JOptionPane.showInputDialog("Enter times per day for medication " + ":"));
            intakeInstructions = JOptionPane.showInputDialog("Enter intake instructions for medication " + ":");
     

        // Create a MedicationTracker
      

        // Get reminder times from user
        int numReminders = Integer.parseInt(JOptionPane.showInputDialog("Enter the number of reminders:"));

        String reminderTimes;
        
            reminderTimes = JOptionPane.showInputDialog("Enter time for reminder " + ":");
        
        // Create a ReminderService


        // Create a MedicationHistory and add the medication to the history
        MedicationHistory medicationHistory = new MedicationHistory();
       

        // Print total medications taken
        JOptionPane.showMessageDialog(null, "Reminder set Successful");*/
    }
    
}
